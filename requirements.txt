# FaceOff Requirements - Optimized for Performance
# Tested with Python 3.11 for best performance (10-60% faster than 3.10)

# Deep Learning & CV - Compatible versions for Python 3.11
# Note: PyTorch 2.6+ required for transformers library security (CVE-2025-32434)
# torch.load with weights_only=True requires torch >= 2.6
torch>=2.6.0
torchvision>=0.21.0  # Compatible with torch 2.6+
torchaudio>=2.6.0    # Compatible with torch 2.6+
onnxruntime-gpu>=1.23.0
insightface>=0.7.3
opencv-python>=4.10.0,<4.11.0
numpy>=1.21.2,<2.0  # Keep 1.x for AI library compatibility

# Image/Video Processing - Stable versions
Pillow>=10.0.0,<12.0  # <12.0 for Gradio compatibility
moviepy>=1.0.3,<2.0  # Use stable 1.x branch
imageio>=2.31.0,<2.32.0
imageio-ffmpeg>=0.6.0  # Updated: fixes pkg_resources deprecation warning
albumentations>=2.0.8  # Updated: latest version with stability improvements

# UI - Modern Gradio
gradio>=5.49.0

# Enhancement Models - Latest compatible
realesrgan>=0.3.0
gfpgan>=1.3.8
basicsr>=1.4.2
facexlib>=0.3.0
transformers>=4.36.0  # For SwinIR/Swin2SR models (alternative to Real-ESRGAN)
codeformer-pip>=0.0.4  # CodeFormer face restoration (alternative to GFPGAN)

# Performance & GPU Acceleration
# TensorRT provides 2-3x faster inference (optional but recommended)
# Install with: pip install tensorrt==10.7.0
tensorrt>=10.0.0

# NVIDIA CUDA libraries for TensorRT support (install if TensorRT errors occur)
# These are optional - only needed if you see "missing DLL" errors
nvidia-cublas-cu12>=12.0.0
nvidia-cudnn-cu12>=9.0.0
nvidia-cufft-cu12>=11.0.0
nvidia-cusparse-cu12>=12.0.0
nvidia-cusolver-cu12>=11.0.0
nvidia-curand-cu12>=10.0.0

# Core Utilities
PyYAML>=6.0
tqdm>=4.66.0
#python-magic-bin>=0.4.14  # File type detection (Windows)
ffmpeg-python>=0.2.0
gdown>=5.0.0  # Model downloads
lpips>=0.1.4  # Perceptual loss
uvicorn>=0.38.0  # Web server
fastapi>=0.121.0  # API framework
onnx>=1.19.0
onnxoptimizer>=0.3.13
decorator>=4.4.2

# Optional but recommended for better performance
# conda install -c conda-forge dlib  # Face landmarks
