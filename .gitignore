# --- Python build artifacts ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.pkl
*.h5

# --- Logs (all log files including rotated backups) ---
*.log
*.log.*

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Gradio temp files ---
.gradio/

# --- Application runtime directories ---
inputs/
outputs/
temp/
temp_gif_frames/
temp_gif_enhanced/

# --- Cache directories ---
cache/
*.cache
tensorrt_cache/

# --- Tests (internal testing files, not to be committed) ---
tests/

# --- Config/environment for local machine only ---
*.env

# --- Model weights (large files) ---
# (Commit ONLY if under 100MB or tracked via git-lfs)
models/**/*.pth
models/**/*.pt
models/**/*.onnx

# GFPGAN weights (large model files)
gfpgan/

# But keep your **main ONNX swapper** (if under 100MB) by explicitly negating
!inswapper_128.onnx

# --- External git repos inside project (prevent nested repos issues) ---
external/**/.git

# --- OS / Editor junk ---
.DS_Store
Thumbs.db
*.swp

# --- Node / frontend (if present later) ---
node_modules/
dist/
build/
